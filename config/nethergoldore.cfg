# Configuration file

ore_drops {
    # Maximum amount of experience drop from the Ore (default: 2)
    I:exp_drop_max=2

    # Minumum amount of experience drop from the Ore (default: 0)
    I:exp_drop_min=0

    # Maximum nuggets drop from the Ore (without fortune). Every Level of fortune increases this value by 1 (default: 6)
    I:nuggets_per_ore_max=6

    # Minumum nuggets drop from the Ore (without fortune). Every Level of fortune increases this value by 1 (default: 4)
    I:nuggets_per_ore_min=4
}


ore_generation {
    # Number of blocks generated per vein (not exact) (default: 22)
    I:block_per_vein=22

    # The maximum height (Y) to try to generate Veins (default: 128)
    I:max_Y=128

    # The minimum height (Y) to try to generate Veins (default: 0)
    I:min_Y=0

    # Number of veins that have to try to spawn per chunk, values below 1 will be chance per chunk to spawn. (By default there's 80% chance for a vein to spawn per chunk) (default: 0.800000011920929)
    D:vein_per_chunk=0.800000011920929
}


ore_properties {
    # Chance for the ore to set on fire the player. Set to 0 to disable (default: 0.0)
    D:ignite_player_chance=0.0

    # Seconds that the player will be set on fire (default: 0)
    I:ignite_player_seconds=0

    # Chance for zombie pigman to get aggroed when a nether gold ore is mined. Set to 0 to disable (default: 5.0)
    D:pigman_aggro_chance=0

    # Radius (from ore mined) for zombie pigman to get aggroed when a nether gold ore is mined
    # The radius has to be intended as a cube and not a sphere (default: 24.0)
    D:pigman_aggro_radius=24.0
}


